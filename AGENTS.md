# Agent instructions

Read `PROTOCOL.adoc` before doing anything else in this repo. It defines how `skills.yaml`, `practice/`, and `tests/` work together — the actual contract this repo runs on, not just a convention.

Note: `README.md` starts out as hand-written onboarding content, but `cmd/reportgen` overwrites it with the live report on its first run (and every run after) -- that's intentional, not a bug. Never hand-edit `README.md` once it's been generated.
